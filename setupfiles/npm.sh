#!/bin/bash

# Install global NPM packages
# list them like this: npm list -g --depth=0

echo "Installing global npm packages"
npm install --global @sanity/cli @sanity/client create-react-app create-react-native-app expo-cli express-generator firebase-tools nodemon react-native-cli typescript